Beta artifacts for the 0.5.0 cut: Cadence-macOS-arm64.zip, Cadence-Windows-Setup.exe,
Cell-0.1.1-macOS-universal.zip, RELEASE-NOTES.md and INSTALL.txt, plus SHA256SUMS.txt.
The page /cadence/beta/ renders its file table from SHA256SUMS.txt, so anything added
here must be added there too.

The seven-plugin bundle zip is deliberately NOT here: at 86 MB it is larger than the
rest of the repo put together and unchanged since the alpha, so it goes to testers by
private link instead. The page says so.
